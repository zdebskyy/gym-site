import React from "react";
// import PropTypes from 'prop-types';

const Schedule = (props) => <div className="ScheduleWrapper">Test content</div>;

// Schedule.propTypes = {
//   // bla: PropTypes.string,
// };

// Schedule.defaultProps = {
//   // bla: 'test',
// };

export default Schedule;
